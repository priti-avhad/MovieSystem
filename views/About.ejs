<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>About - Movie Recommendation System</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet"/>
  <style>
    body {
      background-color: #f8f9fa;
    }

    .card-img-top {
      height: 250px;
      object-fit: cover;
      transition: transform 0.3s ease;
    }

    .card:hover .card-img-top {
      transform: scale(1.03);
    }

    .section-title {
      background: linear-gradient(to right, #4b6cb7, #182848);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-weight: bold;
      font-size: 1.6rem;
      text-align: center;
      margin-bottom: 30px;
    }

    footer {
      margin-top: 50px;
      padding: 20px 0;
      background-color: #343a40;
      color: white;
    }

    .btn-trailer {
      background-color: #e50914;
      border: none;
    }

    .btn-trailer:hover {
      background-color: #c40812;
    }

    .btn-home {
      background-color: #007bff;
      color: white;
    }

    .btn-home:hover {
      background-color: #0056b3;
    }

    @keyframes fadeInSlide {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .header-box {
      padding: 30px 20px;
    }

    .header-title {
      font-size: 1.8rem;
    }

    .header-sub {
      font-size: 1rem;
    }
  </style>
</head>
<body>
  <div class="container mt-4">

    <!-- Smaller Animated About Header -->
    <div class="text-center rounded-4 shadow-lg text-white px-3 py-4 mb-4 header-box" style="
      background: linear-gradient(135deg, #6a11cb, #2575fc);
      border-radius: 2rem;
      animation: fadeInSlide 1.2s ease forwards;
      opacity: 0;
      transform: translateY(20px);
    ">
      <h2 class="mb-2 fw-bold header-title">
        <i class="bi bi-info-circle"></i> About Our Movie System
      </h2>
      <p class="header-sub">
        Discover how we help you find the right movies, effortlessly.
      </p>
    </div>

    <!-- Introduction -->
    <section class="mb-5">
      <p>
        Welcome to our <strong>Movie Recommendation System</strong> – a smart platform built to suggest movies you'll love.
        Using advanced data analysis and user behavior, we continuously improve your viewing experience.
      </p>
      <p>
        Whether you're into thrillers, rom-coms, action blockbusters, or indie gems, we have something for every mood.
        Enjoy personalized recommendations, real user reviews, and immersive trailers – all in one place.
      </p>
    </section>

    <!-- Features -->
    <section class="mb-5">
      <h3 class="mb-3">🎯 Key Features</h3>
      <ul class="features">
        <li>🧠 AI-powered personalized movie suggestions</li>
        <li>📝 View and contribute reviews and ratings</li>
        <li>📜 Full history of your watched movies</li>
        <li>🎞️ Posters, genres, trailers, and cast info</li>
        <li>🔍 Search and explore movies by genre, rating, and popularity</li>
      </ul>
    </section>

    <hr />

    <!-- Recommended Movies -->
    <section>
      <div class="section-title">🔥 Just For You – Recommended Picks</div>
      <div class="row">
        <% if (recommended && recommended.length > 0) { %>
          <% recommended.forEach(movie => { %>
            <div class="col-md-3 mb-4">
              <div class="card h-100 shadow-sm">
                <img src="<%= movie.posterurl %>" class="card-img-top" alt="Movie Poster">
                <div class="card-body d-flex flex-column justify-content-between">
                  <div>
                    <h5 class="card-title"><%= movie.title %></h5>
                    <p class="card-text text-muted mb-2">🎭 <%= movie.genre %></p>
                  </div>
                  <a href="<%= movie.trailerurl || '#' %>" target="_blank" class="btn btn-sm btn-trailer mt-auto">▶ Watch Trailer</a>
                </div>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <p class="text-center text-muted">No recommendations available at this time.</p>
        <% } %>
      </div>
    </section>

    <!-- Go to Home Button -->
    <div class="text-center mt-5">
<a href="/user/dashboard" class="btn btn-home btn-lg shadow">
  🏠 Go to Home
</a>    </div>
  </div>

  <!-- Footer -->
  <footer class="text-center">
    <p class="mb-0">&copy; 2025 Movie Recommendation System. Designed with ❤️ for movie lovers.</p>
  </footer>
</body>
</html>
